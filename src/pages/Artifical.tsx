const Artifical = () => {
    return (
        <div>
            {/*
            A input element will be there in which the user will enter his prompts and then the backend will process that query by sending that query to the LLM which will be having the vector embeddings of all the user posts
            */}
        </div>
    )
}

export default Artifical;
